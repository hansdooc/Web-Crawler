# Web-Crawler
Repository for web crawler with API connection to a NoSQL database

# To run
Clone the repository and create an env file with your credentials to your own MongoDB database

In the terminal, run "uvicorn main:app --reload"

In the API interface, add "/docs" to check for the API endpoints and functionalities.
